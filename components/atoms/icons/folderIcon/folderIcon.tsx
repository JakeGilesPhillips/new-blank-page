import styles from './folderIcon.module.scss';

const FolderIcon = () => {
  return (
    <div className={styles.folderIcon}>
      <div className={styles.folderIcon_1} />
    </div>
  );
};

export default FolderIcon;
