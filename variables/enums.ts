export enum WindowType {
  Home = 0,
  Folder = 1, 
  Page = 2,
  Browser = 3,
  Mail = 4,
  Error = 5,
}

export enum Position {
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
  Center = 'center',
}

export enum Size {
  XS,
  S,
  M,
  L,
  XL,
}